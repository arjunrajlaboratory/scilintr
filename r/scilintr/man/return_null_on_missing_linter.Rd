% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R041-return-null-on-missing.R
\name{return_null_on_missing_linter}
\alias{return_null_on_missing_linter}
\title{Flag \code{if (!file.exists(path)) return(NULL/NA)}.}
\usage{
return_null_on_missing_linter()
}
\description{
This is a silent fallback wearing a different costume -- the explicit
fallback rule (R007) catches \code{try(...)} swallowing, but it doesn't
catch the cleaner-looking \code{if (!file.exists(path)) return(NULL)}.
Same end state: the missing input is treated as an acceptable empty
signal and propagates downstream.
}
\details{
Detection: an \verb{if(!file.exists(...))} whose body is -- or contains --
a \code{return(NULL)}, \code{return(NA)}, \code{return(NA_real_)}, etc. Returns of
other values (\code{return(0)}, \code{return(x)}) are not flagged.

Ported from the Python rule \code{return-none-on-missing-input}.
}
\keyword{internal}
