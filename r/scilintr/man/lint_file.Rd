% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lint.R
\name{lint_file}
\alias{lint_file}
\title{Lint a single R file.}
\usage{
lint_file(path, config = NULL)
}
\arguments{
\item{path}{Path to a \code{.R} file.}

\item{config}{Optional configuration list (loaded from \code{.scilintr.yml}).}
}
\value{
A list of \code{scilintr_finding} records.
}
\description{
Runs every registered per-file linter against \code{path}, converts the
resulting \code{lintr::Lint} objects to \code{scilintr_finding} records, and
applies the \code{ANALYSIS_OK[...]} waiver filter.
}
\examples{
# Lint a tiny self-contained file in the session temp directory.
tmp <- tempfile(fileext = ".R")
writeLines("x <- 1", tmp)
findings <- lint_file(tmp)
length(findings)
unlink(tmp)
}
