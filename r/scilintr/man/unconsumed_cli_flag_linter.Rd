% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R042-unconsumed-cli-flag.R
\name{unconsumed_cli_flag_linter}
\alias{unconsumed_cli_flag_linter}
\title{Flag \code{optparse::make_option("--kebab-name", ...)} calls whose
parsed dest is never read.}
\usage{
unconsumed_cli_flag_linter()
}
\description{
Walks every \code{make_option("--kebab", ...)} (with or without the
\verb{optparse::} qualifier), computes the destination name optparse
would assign (\code{--kebab-name} -> \code{kebab_name}), then checks whether
\emph{any} \verb{<args>$<dest>} or \verb{<args>[["<dest>"]]} access reads that
name -- where \verb{<args>} is the variable bound to a \code{parse_args(...)}
result. If no parse_args() anchor exists in the file we can't tell
what's consumed, so the file is skipped.
}
\details{
Ported from the Python rule \code{unconsumed-cli-flag}.
}
\keyword{internal}
