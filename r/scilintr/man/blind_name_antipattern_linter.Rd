% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R040-blind-name-antipattern.R
\name{blind_name_antipattern_linter}
\alias{blind_name_antipattern_linter}
\title{R040 -- "no-circularity" / "blind" name antipattern}
\usage{
blind_name_antipattern_linter()
}
\description{
A function whose name contains \code{blind}, \code{no_circularity}, \code{unsupervised},
\code{label_free}, \code{independent}, or \code{honest} is asserting that it operates
without consulting labels / ground truth. If the body nevertheless
references known label columns (e.g. \code{legacy_branch2_clone}, \code{truth_c1},
\code{cell_type}) or label files (e.g. \code{cell_labels.tsv}), the lexical
promise of the name is contradicted by the body -- a classic source of
silent circularity in unsupervised analyses.
}
\details{
Detection (v1, hardcoded patterns):
\itemize{
\item function defs found via \verb{<- function(...)}, \verb{<<- function(...)}, or
\verb{= function(...)} (the \code{equal_assign} form).
\item a function name matching \code{(blind|no_circularity|unsupervised| label_free|independent|honest)} is required to trigger inspection.
\item body label refs: \verb{$<label>} where the symbol matches a known label
column, or any STR_CONST mentioning \code{cell_labels} / \verb{labels.<ext>}.
}

Reports at the line of the offending label reference (not the
function definition), so a waiver placed near the body works as
expected. A \verb{# ANALYSIS_OK[blind-name]:} comment within the waiver
window suppresses the finding via the shared waiver layer.
}
\keyword{internal}
