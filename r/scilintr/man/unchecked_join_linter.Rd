% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R003-unchecked-join.R
\name{unchecked_join_linter}
\alias{unchecked_join_linter}
\title{Flag join/merge calls without a follow-up cardinality assertion.}
\usage{
unchecked_join_linter()
}
\description{
Detects calls to \code{left_join}, \code{right_join}, \code{inner_join}, \code{full_join},
\code{anti_join}, \code{semi_join}, or \code{merge}. If the file contains a row-count
or duplicate-key check (\code{stopifnot(...)}, \code{anyDuplicated}, or a
\code{validate = ...} argument), the rule treats the joins as covered and
emits nothing. Otherwise, every join call is flagged.
}
\details{
V1 is intentionally file-level and trigger-happy. Upstream assertions
in another file are handled by the orchestrator's \code{ANALYSIS_OK[join]}
waiver layer, not here.
}
\keyword{internal}
