% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R004-positional-alignment.R
\name{positional_alignment_linter}
\alias{positional_alignment_linter}
\title{Flag positional re-indexing of a dataframe by row/column count of another.}
\usage{
positional_alignment_linter()
}
\description{
Detects patterns like \code{metadata <- metadata[1:ncol(counts), ]} or
\code{metadata[seq_len(ncol(counts)), ]} where rows of one structure are
positionally trimmed/aligned to the column or row count of another.
Such alignment is fragile: it silently succeeds even when the two
structures are in different orders. ID-based alignment
(e.g. \code{counts[, metadata$sample_id]}) is preferred.
}
\details{
V1 finds any single-bracket access whose row-index expression contains
a call to \code{ncol}, \code{nrow}, or \code{seq_len}. Legitimate uses are handled
by the orchestrator's \code{ANALYSIS_OK[id-alignment]} waiver layer.
}
\keyword{internal}
