% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R012-label-in-blind-stage.R
\name{label_in_blind_stage_linter}
\alias{label_in_blind_stage_linter}
\title{Flag references to label / ground-truth column names in files that
are tagged \verb{# STAGE: selection} (or any non-evaluation blind stage).}
\usage{
label_in_blind_stage_linter()
}
\description{
Selection-stage code (PCA, HVG, clustering, embedding, etc.) must be
blind to outcome/label columns; touching \code{metadata$treatment} while
choosing genes leaks the answer key into the unsupervised pipeline.
}
\details{
Stage detection: scan the first 10 lines of the file for a comment
\verb{# STAGE: <name>}. If \verb{<name>} is \code{evaluation} (or no tag is present)
the linter does not fire. Anything else (including \code{selection}) is
treated as a blind stage.

Detection patterns (label names hardcoded for v1):
\itemize{
\item \verb{metadata$<label>}     -- \code{OP-DOLLAR} followed by \code{SYMBOL} matching a label
\item \code{df[["<label>"]]}      -- string literal inside \verb{[[ ]]} matching a label
\item \verb{pull(<label>)}        -- bare \code{SYMBOL} argument to \code{pull()}
}

[ ]: R:\%20
}
\keyword{internal}
