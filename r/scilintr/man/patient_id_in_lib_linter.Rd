% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R021-patient-id-in-lib.R
\name{patient_id_in_lib_linter}
\alias{patient_id_in_lib_linter}
\title{Flag hardcoded patient/sample identifiers (and A191-specific SNP literals)
in files declared \verb{# STAGE: library}.}
\usage{
patient_id_in_lib_linter()
}
\description{
Stage detection: the rule scans the first 10 lines of the file for a
\verb{# STAGE: <name>} directive. If \verb{<name>} is not \code{library}, the rule
returns no findings. This keeps the rule scoped to sample-agnostic
library helpers; analysis scripts can legitimately mention \code{"A191"},
\code{191L}, etc.
}
\details{
v1 forbidden literal set (hardcoded):
\itemize{
\item NUM_CONST: \code{191}, \code{191L}, \code{193}, \code{193L}
\item STR_CONST: \code{"A191"}, \code{"A193"}
\item STR_CONST matching SNP-name pattern \verb{^X\\d+\\.\\d+[A-Z]+\\.[A-Z]+$}
(e.g., \code{"X17.76565019G.A"} -- R-mangled SNP-id, which is necessarily
dataset-specific and should not live in library code).
}

Waiver suppression (\code{ANALYSIS_OK[sample-specific-default]} and similar)
is applied by the orchestrator, not here -- this rule fires on every
offending literal regardless of nearby waiver comments.
}
\keyword{internal}
