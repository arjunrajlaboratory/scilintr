% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R027-env-validator-asymmetry.R
\name{env_validator_asymmetry_linter}
\alias{env_validator_asymmetry_linter}
\title{R027 -- Asymmetric env_* validators within a single file}
\usage{
env_validator_asymmetry_linter()
}
\description{
Flag when a single source file defines two or more \verb{env_*}-named
helper functions that mix two failure styles: some return a default
(silent fall-through) while others halt loudly via \code{stop()}.
Inconsistency here breeds silent misconfiguration: callers cannot
predict whether a missing env var crashes or quietly drifts.
}
\details{
Pragmatic v1: regex over the file text plus a top-level XML check.
Emit a single lint at the line of the FIRST \verb{env_*} function definition
in the file (the source_expression covering that def is the only one
that emits, to keep results stable across lintr's per-expression model).
}
\keyword{internal}
