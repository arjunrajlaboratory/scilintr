% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R039-constant-gates-recursion.R
\name{constant_gates_recursion_linter}
\alias{constant_gates_recursion_linter}
\title{R039 -- Recursive calls with constant hyperparameters across depth}
\usage{
constant_gates_recursion_linter()
}
\description{
Flag a recursive function whose self-call passes one of the function's
own formal arguments through unchanged. When a hyperparameter that was
tuned at the root is inherited verbatim at every recursion depth, the
parent-tuned gate becomes a smuggled default at child nodes -- the
resulting null at depth then reads as "no further structure" when it
really reflects parent-tuned parameters being applied to a different
sub-population (see \code{analysis_lint_strategy.md} section 39).
}
\details{
V1 is intentionally conservative and file-local: it finds top-level
function definitions \code{X <- function(...) { ... }}, looks for direct
self-calls \code{X(...)} in the body, and flags when any argument
expression is a bare SYMBOL whose name matches one of \code{X}'s formal
arguments. A bare formal pass-through (\code{recurse(child, gates)}) fires;
a transformed value (\code{recurse(child, child_gates)}) does not.

Allowed with \verb{# ANALYSIS_OK[constant-gates-at-depth]} waiver.
}
\keyword{internal}
