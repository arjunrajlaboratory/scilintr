% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R037-composite-weights.R
\name{composite_weights_linter}
\alias{composite_weights_linter}
\title{Flag \code{w1*x1 + w2*x2 + w3*x3 + ...} composite scores with literal weights.}
\usage{
composite_weights_linter()
}
\description{
Hand-tuned weights on composite scores are a provenance landmine:
each literal weight is a degree of freedom, and three or more
compounded weights make post-hoc tuning trivially deniable. We flag
any \code{+}-joined expression with three-or-more terms where at least
one term has an explicit numeric coefficient (\verb{<NUM_CONST> * <expr>})
or is itself a bare \verb{<NUM_CONST>}.
}
\details{
Allowed via \code{ANALYSIS_OK[composite-weights]} waiver that cites the
weight-provenance record and a sensitivity check.

Detection:
\itemize{
\item Find each top-level \code{+} \verb{<expr>} (i.e., an \verb{<expr>} whose direct
children include \code{OP-PLUS} but whose parent does NOT, so we only
anchor on the outermost \code{+} in a chain).
\item Recurse through the \code{+} tree to enumerate plus-joined leaf terms.
\item If \verb{>= 3} leaves and at least one leaf is \verb{<NUM_CONST> * <expr>}
or a literal \verb{<NUM_CONST>}, flag the outer expression.
}
}
\keyword{internal}
