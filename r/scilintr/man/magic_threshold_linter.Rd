% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R002-magic-threshold.R
\name{magic_threshold_linter}
\alias{magic_threshold_linter}
\title{Flag magic numeric thresholds in comparison expressions.}
\usage{
magic_threshold_linter()
}
\description{
Detects patterns like \code{padj < 0.05}, \code{counts > 10}, \code{zscores > 3}
where a bare numeric literal sits on either side of a comparison
operator (\code{<}, \code{>}, \code{<=}, \code{>=}, \code{==}, \code{!=}). Named constants
(e.g. \code{padj < FDR_THRESHOLD}) are not flagged because no
\code{NUM_CONST} appears.
}
\details{
Trivial literals (\code{0}, \code{1}, \code{-1}, \code{NA}, \code{TRUE}, \code{FALSE}, \code{Inf},
etc.) are filtered out -- loop sentinels (\code{length(x) > 0L}) and
presence checks (\code{nrow(df) > 0}) are not scientific thresholds.

V1.1 still over-flags relative to the strict spec; legitimate
bare-number cases are handled by the orchestrator's
\code{ANALYSIS_OK[threshold]} waiver.
}
\keyword{internal}
