% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R031-magic-eps-floor.R
\name{magic_eps_floor_linter}
\alias{magic_eps_floor_linter}
\title{Flag \verb{log(pmax(x, <tiny-literal>))} and friends.}
\usage{
magic_eps_floor_linter()
}
\description{
A floor like \code{1e-12} or \code{.Machine$double.eps} (~2.2e-16) inside a
\code{pmax()} immediately before \code{log()} is a numerical-stability
landmine when the floor sits below the data's natural
discretisation grid. The floor then dominates the score and
confuses ranking comparisons. Use a domain-motivated floor (e.g.
half the smallest non-zero increment), not a generic safety
constant.
}
\details{
Detection:
\itemize{
\item Outer call is \code{log} / \code{log1p} / \code{log10} / \code{log2}.
\item Inner call is \code{pmax} / \code{pmax.int}.
\item Second \code{pmax} argument is either a \code{NUM_CONST} with numeric
value \verb{< 1e-6}, or the expression \code{.Machine$double.eps}.
\item \code{pmax(x, 1 / (2 * N))} and similar compound expressions are not
flagged -- the second argument is not a single literal.
}
}
\keyword{internal}
