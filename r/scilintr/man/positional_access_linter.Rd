% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R001-positional-access.R
\name{positional_access_linter}
\alias{positional_access_linter}
\title{Flag raw positional dataframe access by integer literal.}
\usage{
positional_access_linter()
}
\description{
Detects patterns like \code{metadata[, 4]}, \code{df[[3]]}, and \code{df[, 2:5]}
where a bare integer literal sits in the column index slot of a
single-bracket access (after the \emph{first} comma) or as the sole
index of a double-bracket access. Named-constant indices
(e.g. \code{metadata[, TREATMENT_COL_INDEX]}) use SYMBOL, not
NUM_CONST, and are not flagged.
}
\details{
Trivial literals (\code{TRUE}, \code{FALSE}, \code{NA}, \code{0}, \code{1}, \code{Inf}, etc.) are
filtered out -- they parse as \code{NUM_CONST} in xmlparsedata but are
never positional indices. The \code{drop = FALSE} argument of
\code{df[, j, drop = FALSE]} is the canonical false-positive this guards
against.

V1.1 is still trigger-happy on real integer indices. Legitimate
uses are handled by the orchestrator's
\code{ANALYSIS_OK[positional-access]} waiver layer.
}
\keyword{internal}
