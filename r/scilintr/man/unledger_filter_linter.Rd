% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R005-unledger-filter.R
\name{unledger_filter_linter}
\alias{unledger_filter_linter}
\title{Flag self-assignments that silently narrow a dataframe.}
\usage{
unledger_filter_linter()
}
\description{
Patterns like \verb{df <- df[<cond>, ]}, \code{df <- subset(df, ...)}, and
\code{df <- df \%>\% filter(...)} drop rows without recording how many were
dropped or why. The matched rule (R005 in the strategy doc) is that
filtering is allowed when ledgered -- a sibling \verb{ANALYSIS_OK[<tag>]}
waiver comment plus an observable drop record -- but unannotated
self-narrowing should be surfaced.
}
\details{
Heuristic: LEFT_ASSIGN where the LHS SYMBOL name reappears as a SYMBOL
on the RHS, and the RHS contains either a \code{[} (OP-LEFT-BRACKET) or a
call to \code{filter} / \code{subset}. Waiver-comment suppression is the
responsibility of the cross-file aggregator.
}
\keyword{internal}
