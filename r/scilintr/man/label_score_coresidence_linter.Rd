% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R034-label-score-coresidence.R
\name{label_score_coresidence_linter}
\alias{label_score_coresidence_linter}
\title{Flag a \code{data.frame(...)} literal that mixes label columns with
computed-score columns and is subsequently written to disk via
\code{write.csv} / \code{write_csv} from within a \verb{# STAGE: selection} file.}
\usage{
label_score_coresidence_linter()
}
\description{
Rationale: a CSV that pairs labels with discovery scores propagates
the leakage risk downstream -- any later script that re-reads the
file is "one careless edit away" from training on labels. The
project-prescribed remediation is the two-file split (selection
writes \code{selected_calls.csv}, labels-free; evaluation joins labels in
a separate stage). See \code{analysis_lint_strategy.md} #34.
}
\details{
v1 detection (single file):
\enumerate{
\item Stage gate -- only fire when the first 10 lines contain
\verb{# STAGE: selection}.
\item Find \code{data.frame(...)} calls whose argument list contains both
a SYMBOL_SUB matching a label name and a SYMBOL_SUB matching a
score-name regex.
\item Require the file to also contain a \code{write.csv} /
\code{write_csv} / \code{readr::write_csv} call (proxy for "written to
disk"). Flag at the line of the \code{data.frame} call.
}
}
\keyword{internal}
