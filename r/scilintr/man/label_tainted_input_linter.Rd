% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R035-label-tainted-input.R
\name{label_tainted_input_linter}
\alias{label_tainted_input_linter}
\title{Flag \code{read.csv} / \code{read_csv} / \code{read.table} / \code{read.delim} / \code{fread} calls
in a \verb{# STAGE: selection} file whose path argument's basename matches a
label-tainted pattern (e.g. \verb{gt_*}, \verb{*_oracle_*}, \verb{*_recall_*}, etc.).}
\usage{
label_tainted_input_linter()
}
\description{
Even when a selection-stage script only reads one "harmless" column from
such a file, the rows / panel that ended up in the CSV were chosen using
labels -- so the downstream selection is laundered through the file name.
See analysis_lint_strategy.md #35.
}
\details{
Stage gate: the linter scans the first 10 lines for a \verb{# STAGE: <name>}
marker and fires only when \verb{<name>} is exactly \code{selection}. Untagged or
otherwise-tagged files are left to other rules.

Hardcoded label-tainted regex (v1, applied to \code{basename(path)}):
\verb{(^|_)(gt|oracle|truth|evaluated|recall|label)(_|\\d|\\.)}
This anchors on \verb{_}/start and accepts a trailing digit (so \code{gt17_*.csv}
fires), underscore (so \verb{*_oracle_*.csv} fires), or \code{.} (so \code{gt.csv} would
fire too). Project-config extension is left for a future iteration.

Waivers (e.g. \code{ANALYSIS_OK[oracle-file-read]}) are honoured by the waiver
layer; this linter just emits the raw finding.
}
\keyword{internal}
