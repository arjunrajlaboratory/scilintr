% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R033-label-ref-in-selection.R
\name{label_ref_in_selection_linter}
\alias{label_ref_in_selection_linter}
\title{Flag any reference to label / ground-truth columns from inside a file
tagged \verb{# STAGE: selection}.}
\usage{
label_ref_in_selection_linter()
}
\description{
Selection-stage code (module scoring, clustering, embedding, calling
filters, etc.) must be blind to outcome labels. The project enforces a
two-file split: \code{selected_calls.csv} (labels-free) is written by the
selection-stage script, and \code{selected_calls_evaluated.csv} is produced
downstream by an evaluation-stage script that joins labels. Even a
read-only label reference in a selection-stage file makes the leak
"one careless edit away."
}
\details{
Stage detection: scan the first 10 lines of the file for a comment
\verb{# STAGE: <name>}. The linter fires \emph{only} when \verb{<name>} is
exactly \code{selection}. Other stages (including untagged files) are
handled by other rules (e.g. R012 covers all blind stages).

Detection patterns (label names hardcoded for v1):
\itemize{
\item \verb{df$<label>}        -- \code{OP-DOLLAR} followed by \code{SYMBOL} matching a label
\item \code{df[["<label>"]]}   -- string literal inside \verb{[[ ]]} matching a label
\item \verb{pull(<label>)}     -- bare \code{SYMBOL} argument to \code{pull()} / \code{select()}
}

[ ]: R:\%20
}
\keyword{internal}
