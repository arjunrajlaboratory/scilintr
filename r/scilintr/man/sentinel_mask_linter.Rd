% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R044-sentinel-mask.R
\name{sentinel_mask_linter}
\alias{sentinel_mask_linter}
\title{Flag \verb{<name> <- <expr> != ""} / \verb{<name> <- <expr> == ""}.}
\usage{
sentinel_mask_linter()
}
\description{
The mask is the partitioner downstream code uses; treating \code{""} as
a missingness sentinel hides the upstream contract and silently
drops or keeps rows depending on which side of the comparison the
empty string falls on.
}
\details{
Scope, on purpose:
\itemize{
\item Only the empty-string sentinel is matched. \verb{!= 0} / \code{!is.na(x)}
are out of scope (too common and usually legitimate).
\item Only top-level assignments to a plain symbol. The inline form
\code{df[df$col != "", ]} is R005 (\code{unledger-filter}) territory.
\item Composition with \code{&}, \code{|}, \code{!}, and parentheses is unwrapped so a
compound mask like \code{(df$a != "") & (df$b != "")} still fires.
}

Ported from the Python rule \code{sentinel-mask-assignment}.
}
\keyword{internal}
