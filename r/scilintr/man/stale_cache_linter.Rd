% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R009-stale-cache.R
\name{stale_cache_linter}
\alias{stale_cache_linter}
\title{Flag \verb{if (file.exists(<X>)) return(read*(...))} single-statement bodies.}
\usage{
stale_cache_linter()
}
\description{
A cache short-circuit that returns a previously serialized result
without comparing an input fingerprint is a stale-cache hazard: if
upstream inputs change, the cached value is silently returned and
the analysis quietly drifts from its inputs.
}
\details{
Detection (v1): an \code{if} whose condition calls \code{file.exists(...)} and
whose body is a \emph{single} statement calling \code{return(readRDS(...))}
(or \code{read_rds}, \code{read.csv}, \code{read_csv}). Multi-statement \code{{...}}
bodies are spared on the assumption that the extra statements
implement a fingerprint check -- the orchestrator's waiver layer
handles any remaining false positives.
}
\keyword{internal}
