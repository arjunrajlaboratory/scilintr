% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R029-readcsv-mangling.R
\name{readcsv_mangling_linter}
\alias{readcsv_mangling_linter}
\title{Flag \code{read.csv()} then string-keyed column lookup with mangled chars.}
\usage{
readcsv_mangling_linter()
}
\description{
R's \code{read.csv()} / \code{read.table()} rewrite column names: \code{-}, \code{>},
\code{:} become \code{.}, and names starting with a digit get an \code{X} prefix.
A subsequent \code{df[["17-38733306C>T"]]} or \code{df$`17-38733306C>T`}
then silently returns NULL with no error or warning. Passing
\code{check.names = FALSE} (or using \code{readr::read_csv}) avoids the
rewrite.
}
\details{
Two-phase, single-pass detection:
\enumerate{
\item File-level: if the script contains no \code{read.csv} / \code{read.table}
call, or every such call passes \code{check.names = FALSE}, bail.
\item Per-expression: flag string-literal column lookups
(\code{df[["..."]]}) and backtick-symbol lookups
(\code{df$`...`}) whose name carries \code{-}, \code{>}, \code{:}, or a leading
digit.
}

Both \code{df[["..."]]} (STR_CONST) and \code{df$`...`} (OP-DOLLAR /
SYMBOL) forms are handled.
}
\keyword{internal}
