% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-R036-threshold-near-label.R
\name{threshold_near_label_linter}
\alias{threshold_near_label_linter}
\title{Flag uppercase-constant assignments that are defined within a few lines
of a label-tainted read or label-column reference, inside a file tagged
\verb{# STAGE: selection}.}
\usage{
threshold_near_label_linter()
}
\description{
Selection-stage code must not pick its thresholds/bands by maximizing
a label-aware metric (e.g. ground-truth recall). When a constant like
\code{BAND <- sweep[which.max(sweep$gt_recall), ...]} sits next to a
\code{read.csv("..._gt_...")} call or a label column reference, the
threshold is effectively label-tuned. The fix is to either (a) move
the constant to \code{analysis_constants.yml} with a documented value, or
(b) move the threshold-selection code into a \verb{# STAGE: evaluation}
script and pass the resulting number in by hand.
}
\details{
Stage detection: scan the first 10 lines for \verb{# STAGE: <name>}. Fires
only when \verb{<name>} is exactly \code{selection}. Other stages (including
untagged files) are not flagged here.

Detection: regex on the raw file text.
\itemize{
\item Constant lines: \verb{^[A-Z][A-Z_0-9]+\\\\s*<-}
\item Label-tainted reads: paths/names containing \code{gt}, \code{oracle},
\code{truth}, \code{evaluated}, \code{recall} as a token.
\item Label column refs: a small hardcoded vocabulary (kept in sync
with R033 for consistency).
\item A constant is flagged when at least one label-tainted line falls
within +/- 6 lines of it.
}
}
\keyword{internal}
